function animal(obj){
  var animal ={name: "dog", legs: 4, color: "white"}
   return `This ${obj.color} ${obj.name} has ${obj.legs} legs.`;
}

function animal(obj){
  var animal = {name:"cock",legs:2,color:"red"}
   return `This ${obj.color} ${obj.name} has ${obj.legs} legs.`;
}

function animal(obj){
  var animal = {name:"rabbit",legs:4,color:"gray"}
   return `This ${obj.color} ${obj.name} has ${obj.legs} legs.`;
}
